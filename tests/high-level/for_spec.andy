import(:tests)

describe('for') {
    it('should iterate over a range') {
        var sum = 0;
        for (var i = 0; i < 10; i++) {
            sum += i;
        }
        expect(sum).to_eq(45);
    }
}