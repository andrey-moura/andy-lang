import(:tests);

describe('Standard') {
    describe('::system') {
        it('should adquire the exit code') {
            var exit_code = system('exit 10');
            expect(exit_code).to_eq(10);
        }
    }
}